# ClusterIP — the Ingress routes external traffic to it.
apiVersion: v1
kind: Service
metadata:
  name: web
  labels:
    app: web
spec:
  type: ClusterIP
  selector:
    app: web
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
