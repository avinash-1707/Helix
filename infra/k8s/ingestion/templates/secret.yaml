# The DB connection string carries the password — keep it in a Secret.
apiVersion: v1
kind: Secret
metadata:
  name: ingestion-secrets
  labels:
    app: ingestion
type: Opaque
stringData:
  DATABASE_URL: {{ .Values.secrets.databaseUrl | quote }}
