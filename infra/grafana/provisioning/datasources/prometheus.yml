# Auto-provisioned Grafana datasource — the Prometheus instance in this
# compose stack. Lets dashboards load with zero manual setup.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
