// @helix/api — Fastify API gateway.
// Build-order step 5: conversation CRUD, SSE streaming route,
// Redis context cache. Routes stay thin (src/routes/), business
// logic in src/services/. See context/code-standards.md.

export {};
