// Tailwind CSS v4 runs as a PostCSS plugin. No tailwind.config — the
// design system is defined CSS-first in app/globals.css via @theme.
export default {
  plugins: {
    "@tailwindcss/postcss": {},
  },
};
