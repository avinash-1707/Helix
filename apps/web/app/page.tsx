// Placeholder route — replaced in build-order step 7 by the
// chatbot UI (sidebar, conversation list, streaming message panel).
export default function Page() {
  return <main>Helix — scaffold</main>;
}
